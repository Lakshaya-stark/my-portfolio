export default function RaspberryAscii() {
  return (
    <pre className="text-[#e6af07] text-[30px] leading-none">
      {`
     ┌─────────────────────┐
     │   RASPBERRY PI      │
     │  ────────────────   │
     │  ▓▓▓ CPU ▓▓▓        │
     │                     │
     │  USB   HDMI         │
     │                     │
     │  GPIO GPIO GPIO     │
     └─────────────────────┘
`}
    </pre>
  );
}
